# Ошибки и наблюдения

Файл нужен, чтобы не терять проблемы между чатами и версиями.

## Открытые проблемы

Пока нет подтверждённых критичных открытых проблем.

## Что стоит наблюдать

- Живые подключения Gate.io и OKX могут зависеть от сети и ограничений бирж.
  Если статус снизу показывает ошибку, лучше сохранить текст статуса и
  `debug.log`.
- AsterDEX использует Binance-style протокол. Если конкретный тикер не
  подключается, сначала проверить, торгуется ли он реально на выбранном рынке.
- Комментарии в `Ленте алертов` сейчас живут только в текущем сеансе окна.
  После перезапуска старая лента алертов не восстанавливается.

## Шаблон новой ошибки

```text
Дата:
Версия / ветка:

Что делал:

Что ожидал:

Что получилось:

Фильтры:
- Биржа:
- Символ:
- От $:
- До $:
- Дистанция %:
- Жизнь, с:
- Режим:
- Направление:

Скрин:

debug.log / статус снизу:
```

## Когда проблема исправлена

После исправления перенести запись сюда:

```text
Исправлено:
- дата;
- коротко что было;
- какой тест или проверка теперь закрывает проблему.
```
